{
  "_comment": "Optional fallback only. Prefer ANIMEM_DB_* environment variables — see Config/credentials.php. Copy to config.json on the server if you cannot set environment variables; config.json is git-ignored.",
  "db": {
    "host": "localhost",
    "username": "CHANGE_ME",
    "password": "CHANGE_ME",
    "database": "animem"
  },
  "site": {
    "domain": "animem.org",
    "videoDomain": "animem.hu"
  }
}
