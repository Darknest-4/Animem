# Keeps the dependency graph honest: a package may only import what it declares.
# Hoisting would let apps/api use a transitive dependency it never listed, which
# then breaks the day that dependency's parent drops it.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
